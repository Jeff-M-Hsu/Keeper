import React, { Component } from 'react';

class NotesPage extends Component{
  render(){
    return(
     <div id="notes page" className="nav-page hidden">
            <span>notes page</span>
          </div>
    );
  }
}

export default NotesPage;
